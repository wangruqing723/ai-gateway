# 容器以宿主用户身份运行，保证管理页面能写回挂载的 config.yaml。
# 推荐直接用 ./scripts/up.sh 启动，它会自动用本机 id -u / id -g 生成 .env 并对齐属主。
# 如需手填，复制本文件为 .env 并按本机取值填写：
#   macOS 典型:     501 / 20
#   Linux·WSL 典型: 1000 / 1000
AI_GATEWAY_UID=1000
AI_GATEWAY_GID=1000

# ── 出网代理（可选，默认不走代理）───────────────────────────────
# 仅在「上游需要代理才能访问」时才填，例如公司网络限制出网，或上游域名
# 被 DNS 污染（容器内解析到错误 IP，表现为 dial ... connection refused）。
#
# 关键：宿主机的代理在容器里不能写 127.0.0.1（那指容器自己），要写
# host.docker.internal。compose 已配好该域名的跨平台映射。
#
# 举例：宿主代理监听 127.0.0.1:7897，则容器内应填 7897 端口的下面这行。
#AI_GATEWAY_HTTPS_PROXY=http://host.docker.internal:7897
#AI_GATEWAY_HTTP_PROXY=http://host.docker.internal:7897
#
# 绕过代理的目标（逗号分隔）。留空则用 compose 内置默认值
# localhost,127.0.0.1,::1。
#
# 注意：本机回环是自动排除的，但**容器网络里的服务名不是**。若某个 provider
# 的 baseUrl 指向同一 docker 网络内的另一个容器（如 http://my-llm:8000），
# 一旦开了代理，该请求会被送去代理并失败——必须把服务名也写进 NO_PROXY。
# 指向局域网 IP 的 provider 同理。
#AI_GATEWAY_NO_PROXY=localhost,127.0.0.1,::1,my-llm,192.168.0.0/16
#
# 生效范围：网关所有出网请求——上游转发、vision 图片翻译、Provider 健康
# 检测、上游模型列表查询。改完需重建容器（docker compose up -d
# --force-recreate），单独 restart 不会更新环境变量。
