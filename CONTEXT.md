# 网关运行观测上下文

本上下文统一描述网关请求质量、图片识别降级与运行状态展示，避免把上游可用性、图片识别质量和网关请求成败混为一谈。

## 请求质量与图片识别

**图片识别失败**：网关尝试把请求中的图片转换为文字描述时，该张图片未能得到有效识别结果；它不等同于网关请求失败。

**软降级**：图片识别失败后，请求仍继续交给目标模型处理；图片识别失败本身不改变请求成功或失败的判定。

**图片识别独立计数**：在请求成功率之外，单独统计图片识别尝试与失败情况，使图片识别质量可被观测而不污染上游请求可用性指标。按图片块计数：每个图片块记一次识别尝试，缓存命中计为成功。

**图片识别错误大类**：对图片识别失败原因的少量稳定归类（超时、网络/代理、上游 HTTP 错误、响应解析、图片格式/来源、队列、其他），与截断后的原始错误信息同时展示。

## 运行状态

**熔断打开事件**：某个 Provider 从允许请求的状态转为暂时禁止请求的熔断状态这一事实；它是运行状态变化，不等同于某一次请求的最终错误。

**Provider 健康异常**：健康检测发现某个 Provider 当前不可正常提供网关所需服务的状态；它与请求转发中的熔断状态独立。

**周期健康检测**：按配置间隔自动探测全部 Provider 的后台行为；未配置时不产生任何后台流量，手动检测始终可用。

**状态变化事件**：熔断状态或健康状态发生转变时追加的一条记录；同一 Provider 在恢复之前不重复产生同类事件。

**恢复事件**：熔断或健康状态回到正常时追加的低优先级记录，用于确认异常已结束。

**运行异常区**：仪表盘中集中展示图片识别失败、熔断事件与健康事件的区域；请求终态错误仍归"最近错误"，两者不混排。

**被动展示**：信息保留在监控仪表盘的指标、列表或状态区域中，由使用者查看，不自动推送、不要求确认消除。
