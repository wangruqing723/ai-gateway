## [2026-08-29] S2-8：预览抽屉化后原双滚动前提消失

- 发现于：S2-8 / `cmd/gateway/web/index.html`
- 问题描述：原问题依赖配置预览 `aside` 在 `xl` 分栏未生效时落成第二行；S2-1 已将预览改为脱离文档流的固定抽屉，配置区在 1024–1279px 只剩一个主栏，`lg` 内滚动可以覆盖全部内容。
- 建议：不再把 `main`、配置主栏的 `lg` 滚动门控改到 `xl`，本条无需额外 class 改动；保留现有 `lg` 内滚动以保证中等宽度下内容可达。
- 状态：已决策无需改动，待 1024–1280px 浏览器肉眼验收。本次受限环境无法启动本地 HTTP 服务或取得离线 Playwright CLI，未将静态判断冒充实机结论。
